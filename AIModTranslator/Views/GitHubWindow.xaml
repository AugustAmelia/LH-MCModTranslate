<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:material="using:Material.Icons.Avalonia"
        Title="Импорт из GitHub" Height="300" Width="600"
        WindowStartupLocation="CenterOwner"
        x:Class="AIModTranslator.Views.GitHubWindow">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <material:MaterialIcon Kind="Github" Width="30" Height="30" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="Purple" />
            <TextBlock Text="Импорт мода из GitHub" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Вставьте ссылку на репозиторий мода:" FontWeight="SemiBold" Margin="0,0,0,5" />
        
        <TextBox Grid.Row="2" Text="{Binding RepoUrl}" 
                 Watermark="https://github.com/author/modname" Margin="0,0,0,15" />

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Gray" TextWrapping="Wrap" VerticalAlignment="Top" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <ProgressBar IsIndeterminate="True" Width="25" Height="25" Margin="0,0,15,0"
                         IsVisible="{Binding IsBusy}" />
            <Button Click="OnDownloadClicked" Content="🐙 Скачать и загрузить" />
        </StackPanel>
    </Grid>
</Window>
